'use strict'

var jasmine = require('jasmine').expect;

var words = require('./words.js').words;

describe("wordFrequency()", function() {
  it("sentences.forEach", function() {
    var expectedSentence = { [tweets]};
    expect(wordFrequency("sentences")).toEqual(instanceof Array);
  });

  
});

